return {
    height = 400, -- size of the console, can be used by the styles
    width = 600,
    font = nil,
    limit = nil, -- limit of logs, nil for no limit
    openkey = "f1", -- default key to open the console
    cursors = true, -- toggle the console's cursors
}